--- a/hermit_vessel.py
+++ b/hermit_vessel.py
@@ -22,16 +22,15 @@
 import json
 import os
 import sys
 import uuid
-import sqlite3
-import hashlib
 from datetime import datetime, timezone
 from pathlib import Path
 from typing import Optional, Any


 # ── Paths ────────────────────────────────────────────────────────────────────

 HERE = Path(__file__).parent.resolve()
@@ -671,11 +670,18 @@ def main():
 # ── CLI ──────────────────────────────────────────────────────────────────────


 def print_launch_report(report: dict):
-    """Pretty-print a launch report."""
+    """Pretty-print a launch report with formatted status display.
+
+    Args:
+        report: Launch report dictionary containing vessel status and steps
+
+    Displays a formatted table showing vessel launch progress with icons
+    and status indicators for each step.
+    """
     print()
     print("╔══════════════════════════════════════════════════════════╗")
     print("║  🦀  HERMIT VESSEL LAUNCH                               ║")
@@ -698,13 +704,25 @@ def print_launch_report(report: dict):
         print("Some steps had issues. See report above for details.")


-def main():
+def main() -> None:
+    """Main CLI entry point for Hermit vessel operations.
+
+    Commands:
+        (none)          Full vessel launch with handshake
+        --status, -s    Check connection status
+        --send-query    Send a research query to Hermit
+        --ingest-captures  Send recent captures as observations
+        --read-incoming Read bottles from Hermit
+        --help, -h      Show help message
+
+    Exits with code 1 on launch failure.
+    """
     vessel = HermitVessel()

     if len(sys.argv) > 1:
         cmd = sys.argv[1]

         if cmd == "--status" or cmd == "-s":
             status = vessel.status()
             print(json.dumps(status, indent=2, default=str))
             return

--- a/memory_bridge.py
+++ b/memory_bridge.py
@@ -18,7 +18,6 @@

 import json
 import logging
 import sys
-import time
 from pathlib import Path
 from typing import Any, Optional

@@ -129,6 +128,15 @@ def process_capture(json_path: Path) -> dict[str, Any]:
     }


-def tick() -> dict[str, Any]:
+def tick() -> dict[str, Any]:
+    """Periodic tick function called from analyzer.py's run_forever loop.
+
+    Performs automated memory maintenance:
+    - Checks tide pool for auto-flush
+    - Prunes decayed stipes entries
+    - Migrates holdfast queue
+
+    Returns:
+        Dictionary with boolean status flags for each memory layer
+    """
     report = {"tide_ok": False, "stipes_ok": False, "holdfast_ok": False}

     # Tide pool flush check
@@ -173,7 +181,14 @@ def tick() -> dict[str, Any]:
     return report


-def migrate_holdfast() -> dict[str, Any]:
+def migrate_holdfast() -> dict[str, Any]:
+    """Manually drain the holdfast graduation queue.
+
+    Forces immediate migration of queued candidates from stipes to holdfast
+    permanent storage. Useful for testing or manual persistence triggers.
+
+    Returns:
+        Dictionary with migration count and total holdfast size
+    """
     from holdfast import Holdfast
     hf = Holdfast()
     count = hf.migrate()
@@ -181,7 +196,13 @@ def migrate_holdfast() -> dict[str, Any]:
     return {"migrated": count, "total": len(hf)}


-def status() -> dict[str, Any]:
+def status() -> dict[str, Any]:
+    """Full memory system diagnostic across all three layers.
+
+    Returns comprehensive status for tide_pool, stipes, and holdfast
+    including entry counts, errors, and graduation queue length.
+
+    Returns:
+        Nested dictionary with each layer's status and statistics
+    """
     try:
         tp = get_tide_pool()
         tp_status = tp.status()
@@ -213,6 +234,15 @@ def status() -> dict[str, Any]:
     }


+def cli() -> None:
+    """CLI entry point for memory bridge operations.
+
+    Commands:
+        process <json_path>  Process capture analysis JSON
+        tick                 Run periodic maintenance tick
+        migrate              Manual holdfast migration
+        status               Full memory system status
+
+    Exits with code 1 on unknown command.
+    """
     if len(sys.argv) < 2 or sys.argv[1] in ("-h", "--help", "help"):
         print("Usage: python memory_bridge.py <command> [args]")
         print()
